// The harness core contracts (Job, domain definition, agent runtime, decision
// engine, workflow node, capability registry, fallback envelope) are specified
// in docs/milestones/build-plan.md section 5 and land in Milestone 1.
//
// This package is deliberately empty until then. Adding placeholder
// abstractions now would invent a boundary before the contracts that define it
// exist, so the package exports nothing and only proves that the build,
// typecheck and packaging pipeline works end to end.
export {};
