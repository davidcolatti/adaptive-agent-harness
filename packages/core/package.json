{
  "name": "@internal/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Harness core contracts: JSON value model, execution context, trace boundary and error taxonomy.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "@internal/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@internal/config": "workspace:*",
    "vitest": "5.0.1"
  }
}
